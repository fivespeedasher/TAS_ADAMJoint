#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <string>
#include <locale>
#include <codecvt>
#include <vector>

std::vector<uint16_t> gbk_hex(const std::string& gbk_str);

#endif // UTILS_H